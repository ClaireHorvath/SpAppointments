const makeAppointmentForm = document.querySelector('form')

const makeAppointmentDate = document.querySelector("#make-appointment-date")
const makeAppointmentTime = document.querySelector("#make-appointment-time")
const makeAppointmentSelectService = document.querySelector("#make-appointment-select-service")

function handleSubmit(evt) {
	evt.preventDefault()

	.textContent

	alert("Appointment booked!")
}

let form = document.querySelector('#make-appointment')

makeAppointmentForm.addEventListener('submit', handleSubmit)
